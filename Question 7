#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
    int i;

    printf("Number  Square  Cube \n");

    for(i=0; i<11; i++)
        {
        printf("%d\t%.0lf\t%.0lf\n", i, pow(i,2), pow(i,3));
        }

    return 0;
}
