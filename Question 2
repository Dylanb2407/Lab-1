#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x[10];
    int i, sum=0;

    for(i=0;i<10; i++){
        printf("Enter Value %d = ", i+1);
        scanf("%d", &x[i]);
        sum += x[i];
        printf("     Total= %d \n", sum);
    }
    return 0;
}
